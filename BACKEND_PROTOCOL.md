# XURA — Протокол взаимодействия клиент ↔ бэкенд

> Версия: 1.2  
> Дата: 2026-06-30  
> Сеть: XRPL Mainnet (`wss://xrplcluster.com`)  
> Источник истины: исходный код ветки `main`

---

## 1. Транспорт

Все игровые взаимодействия — это **XRP Payment-транзакции** в сети XRPL.  
Данные передаются в поле **MemoData** (UTF-8, закодировано в HEX uppercase).

```
MemoData = bytes(text, "utf-8").hex().upper()
```

Никаких HTTP-запросов между клиентом и игровым сервером нет — только on-chain транзакции.

---

## 2. Адреса игровых серверов

| Игра | Адрес назначения |
|------|-----------------|
| Угадай цвет | `rGrEJZaBFYhPGuyM7NiJbJw2yXVB9vJHah` |
| Угадай число | `rfcMxSEz4JP8zj65LU5Nw9hKfpEaD6Ss9` |
| Рулетка | `rGrEJZaBFYhPGuyM7NiJbJw2yXVB9vJHah` |
| Реферальная программа | `rGrEJZaBFYhPGuyM7NiJbJw2yXVB9vJHah` |

> Угадай цвет, рулетка и реферальная программа используют **один адрес**.  
> Тип операции различается по содержимому MemoData.

---

## 3. Общий формат сообщений

### 3.1 Клиент → Сервер (ставка / заявка)

```
XRPL Payment:
  Destination: <адрес сервера>
  Amount:      <сумма в дропах>  (1 XRP = 1 000 000 drops)
  Memos:
    Memo:
      MemoData: HEX("<текст команды>")
```

### 3.2 Сервер → Клиент (ответ)

```
XRPL Payment:
  Destination: <адрес кошелька игрока>
  Amount:      <выплата в дропах>
  Memos:
    Memo:
      MemoData: HEX("<ответ>")
```

Клиент принимает ответ через **WebSocket-подписку** на поток `transactions` для своего адреса.  
Декодирование: `bytes.fromhex(MemoData).decode("utf-8").upper()` — клиент всегда переводит в **uppercase**.

Существует **два семейства форматов** ответов сервера:

| Семейство | Используется для | Пример |
|-----------|-----------------|--------|
| `CMD:{значение}` | Угадай цвет, Угадай число | `WIN:17`, `LOSE:0` |
| `RLT:{…}` | Рулетка, лотереи, реферальная программа | `RLT:17:WIN`, `RLT:LOTTO:DAILY:2026-06-30` |

---

## 4. Игра «Угадай цвет» (Guess the Color)

### 4.1 Ставки

| Параметр | Значение |
|----------|---------|
| Адрес сервера | `rGrEJZaBFYhPGuyM7NiJbJw2yXVB9vJHah` |
| Минимальная ставка | **0.1 XRP** |
| Максимальная ставка | **100 XRP** |
| Точность суммы | до **6 знаков** после запятой |
| Множитель выплаты | **×2** |

### 4.2 Мемо ставки (клиент → сервер)

```
BET:RED:{referral}    — ставка на красный цвет
BET:BLK:{referral}    — ставка на чёрный цвет
```

| Поле | Значение |
|------|---------|
| `BET` | тип операции — игровая ставка |
| `RED` / `BLK` | выбранный цвет |
| `{referral}` | реферальный код игрока; `"0"` если реферала нет |

**Примеры:**
```
BET:RED:0              — ставка на красный, без реферала
BET:BLK:XURA99        — ставка на чёрный, реферал XURA99
```

### 4.3 Ожидаемые ответы сервера

| Ответ MemoData | Смысл | Выплата |
|---------------|-------|---------|
| `WIN:{число}` | Угадал цвет | `сумма_ставки × 2` XRP |
| `LOSE:{число}` | Не угадал | **12 дропов** (символически) |
| `JKPT:{число}` | Джекпот (клиент отображает как WIN) | сумма выплаты |
| `LOTO:{число}` | Лото-выигрыш (клиент отображает как WIN) | сумма выплаты |

- `{число}` — выпавшее число рулетки (0–36), которое клиент отображает на колесе.
- 0 — зелёное зеро, оба цвета (RED и BLK) **проигрывают** при выпадении нуля.

---

## 5. Игра «Угадай число» (Guess the Number)

### 5.1 Ставки

| Параметр | Значение |
|----------|---------|
| Адрес сервера | `rfcMxSEz4JP8zj65LU5Nw9hKfpEaD6Ss9` |
| Диапазон числа | **1–36** (0 не доступен для ставки) |
| Минимальная ставка | **0.1 XRP** |
| Максимальная ставка | **100 XRP** |
| Точность суммы | до **6 знаков** после запятой |
| Множитель выплаты | **×36** |

### 5.2 Мемо ставки (клиент → сервер)

```
BET:N:{число}:{referral}
```

| Поле | Значение |
|------|---------|
| `BET` | тип операции — игровая ставка |
| `N` | тип игры — число |
| `{число}` | выбранное число от 1 до 36 |
| `{referral}` | реферальный код; `"0"` если нет |

**Примеры:**
```
BET:N:7:0              — ставка на число 7, без реферала
BET:N:17:XURA99       — ставка на число 17, реферал XURA99
BET:N:36:ref5000      — ставка на число 36
```

### 5.3 Ожидаемые ответы сервера

| Ответ MemoData | Смысл | Выплата |
|---------------|-------|---------|
| `WIN:{число}` | Число угадано | `сумма_ставки × 36` XRP |
| `LOSE:{число}` | Число не угадано | **12 дропов** (символически) |
| `JKPT:{число}` | Джекпот (→ WIN) | сумма выплаты |
| `LOTO:{число}` | Лото (→ WIN) | сумма выплаты |

- `{число}` — выпавшее число (то, которое сгенерировал сервер). Клиент сравнивает с тем, что поставил игрок.
- Клиент не делает самостоятельного расчёта выигрыша — он доверяет `delivered_amount` из ответной транзакции.

---

## 6. Игра «Рулетка» (Roulette)

### 6.1 Параметры

| Параметр | Значение |
|----------|---------|
| Адрес сервера | `rGrEJZaBFYhPGuyM7NiJbJw2yXVB9vJHah` |
| Мин. ставка на позицию | **0.1 XRP** |
| Макс. суммарная ставка | **100 XRP** |
| Тип рулетки | Европейская (0–36) |

### 6.2 Мемо ставки (клиент → сервер)

```
BET:R:{код1}@{сумма1},{код2}@{сумма2},...:{referral}
```

Итоговая сумма транзакции = сумма всех ставок в дропах.

**Примеры:**
```
BET:R:n17@1.5:0                          — прямая ставка на 17, 1.5 XRP
BET:R:r@2.0,b@2.0:XURA99               — красные + чёрные по 2 XRP
BET:R:n5@1.5,r@2.0,d1@0.5:ref123       — три ставки
```

### 6.3 Таблица кодов ставок рулетки

| Код | Ставка | Условие выигрыша | Множитель |
|-----|--------|-----------------|-----------|
| `n0` | Число 0 | выпало 0 | **×36** |
| `n1`–`n36` | Число 1–36 | выпало то число | **×36** |
| `r` | Красные | красное число | **×2** |
| `b` | Чёрные | чёрное число | **×2** |
| `o` | Нечётные | нечётное, ≠ 0 | **×2** |
| `e` | Чётные | чётное, ≠ 0 | **×2** |
| `l` | 1–18 | 1 ≤ число ≤ 18 | **×2** |
| `h` | 19–36 | 19 ≤ число ≤ 36 | **×2** |
| `d1` | 1-я дюжина | 1 ≤ число ≤ 12 | **×3** |
| `d2` | 2-я дюжина | 13 ≤ число ≤ 24 | **×3** |
| `d3` | 3-я дюжина | 25 ≤ число ≤ 36 | **×3** |
| `c1` | 1-я колонка | число % 3 == 1 | **×3** |
| `c2` | 2-я колонка | число % 3 == 2 | **×3** |
| `c3` | 3-я колонка | число % 3 == 0 | **×3** |

**Чёрные числа:** 2, 4, 6, 8, 10, 11, 13, 15, 17, 20, 22, 24, 26, 28, 29, 31, 33, 35  
**0** — зелёное зеро; все внешние ставки (r, b, o, e, l, h, d1–d3, c1–c3) проигрывают.

### 6.4 Ответы сервера — рулетка (формат `RLT:`)

Все ответы сервера по рулетке используют префикс **`RLT:`**.

#### Обычный результат ставки

```
RLT:{число}:{WIN|LOSE}
```

| Ответ MemoData | Смысл | Выплата |
|---------------|-------|---------|
| `RLT:17:WIN` | Хотя бы одна ставка выиграла | итоговая выплата по выигравшим позициям |
| `RLT:0:LOSE` | Все ставки проиграли | **12 дропов** |

- `{число}` — выпавшее число (0–36). Колесо останавливается именно на нём.
- `WIN` / `LOSE` — только эти два значения; регистр не важен (клиент приводит к uppercase).

#### Лотереи

```
RLT:LOTTO:DAILY:{дата}          — ежедневная лотерея
RLT:LOTTO:SUPER:{год-месяц}     — ежемесячная супер-лотерея
```

| Ответ MemoData | Смысл | Выплата |
|---------------|-------|---------|
| `RLT:LOTTO:DAILY:2026-06-30` | Победитель ежедневной лотереи | джекпот |
| `RLT:LOTTO:SUPER:2026-06` | Победитель ежемесячной лотереи | джекпот |

- Дата и месяц используют символ `-`, а не `:` — парсинг по `:` даёт чистое значение в `parts[3]`.
- Колесо останавливается на **0** (символически, числа ставки нет).
- Клиент показывает экран джекпота (`CONGRATULATIONS … LOTTO`).

---

## 7. Реферальная программа

### 7.1 Стать рефералом

| Параметр | Значение |
|----------|---------|
| Адрес сервера | `rGrEJZaBFYhPGuyM7NiJbJw2yXVB9vJHah` |
| Стоимость | **66 XRP** |
| Мемо | `REF` |

**Транзакция клиента:**
```
Destination: rGrEJZaBFYhPGuyM7NiJbJw2yXVB9vJHah
Amount:      66 000 000 drops  (66 XRP)
MemoData:    HEX("REF")        →  "524546"
```

**Ответ сервера:**
```
RLT:REF:CODE:{присвоенный_код}

Примеры:
  RLT:REF:CODE:7483647     — сервер выдал числовой код
  RLT:REF:CODE:XURA0001    — сервер выдал буквенно-цифровой код
```

Клиент сохраняет `{код}` и показывает его пользователю как его реферальный номер.  
Максимальное количество рефералов в системе: **666 666**.

---

### 7.2 Восстановление реферального статуса

| Параметр | Значение |
|----------|---------|
| Адрес сервера | `rGrEJZaBFYhPGuyM7NiJbJw2yXVB9vJHah` |
| Стоимость | **13 XRP** |
| Мемо | `REF:REC` |

**Транзакция клиента:**
```
Destination: rGrEJZaBFYhPGuyM7NiJbJw2yXVB9vJHah
Amount:      13 000 000 drops  (13 XRP)
MemoData:    HEX("REF:REC")    →  "5245463A524543"
```

**Ответ сервера:**
```
RLT:REF:CODE:{восстановленный_код}
```

Сервер должен вернуть код, ранее привязанный к данному адресу кошелька.

---

### 7.3 Вознаграждение рефереру

Когда другой игрок использует реферальный код при ставке, сервер автоматически отправляет вознаграждение владельцу кода:

```
RLT:REF:REWARD:{код}

Пример:
  RLT:REF:REWARD:7483647   — вознаграждение за то, что кодом 7483647 воспользовались
```

| Параметр | Поведение клиента |
|----------|-----------------|
| Flasher открыт (идёт спин) | Вознаграждение **не прерывает** анимацию — доставляется после возврата в MainActivity |
| Flasher закрыт | Открывается Win-экран с суммой вознаграждения |
| `{код}` | Реферальный код, за который пришло вознаграждение |
| Сумма | `delivered_amount` транзакции в XRP |

---

## 8. Сводный формат ответов сервера

### Семейство `CMD:` — Угадай цвет и Угадай число

```
{CMD}:{значение}
```

| CMD | Действие клиента | `{значение}` |
|-----|-----------------|--------------|
| `WIN` | Показать выигрыш | выигрышное число (0–36) |
| `LOSE` | Показать проигрыш | выигрышное число (0–36) |
| `JKPT` | Показать выигрыш (джекпот) | выигрышное число |
| `LOTO` | Показать выигрыш (лото) | выигрышное число |
| `REF` | Сохранить реферальный код | сам код _(устаревший формат, поддерживается)_ |

### Семейство `RLT:` — Рулетка и реферальная программа

```
RLT:{тип}:{…}
```

| Формат MemoData | Действие клиента |
|----------------|-----------------|
| `RLT:{число}:WIN` | Выигрыш в рулетке; колесо на `{число}` |
| `RLT:{число}:LOSE` | Проигрыш в рулетке; колесо на `{число}` |
| `RLT:LOTTO:DAILY:{дата}` | Победитель ежедневной лотереи; колесо на 0 |
| `RLT:LOTTO:SUPER:{год-месяц}` | Победитель ежемесячной лотереи; колесо на 0 |
| `RLT:REF:CODE:{код}` | Реферальный код выдан или восстановлен → экран YourReferral |
| `RLT:REF:REWARD:{код}` | Вознаграждение рефереру → Win-экран (не прерывает Flasher) |

Клиент читает `delivered_amount` из `meta` ответной транзакции — это реальная сумма выплаты.

---

## 9. Как клиент принимает ответ

1. После отправки ставки клиент показывает экран Flasher (вращающееся колесо).
2. Клиент **подписан через WebSocket** на поток транзакций своего адреса (`accounts: [адрес_игрока]`).
3. При поступлении транзакции с `type: "transaction"` и полем `Memos`:
   - Клиент проверяет, что `Destination` — **не** адрес игрового сервера (т.е. это ответ от сервера, а не своя же ставка).
   - Декодирует `MemoData` из HEX в UTF-8, переводит в uppercase.
   - Если мемо начинается с `RLT:` — парсит как RLT-формат (рулетка, лотерея, реферал).
   - Иначе — парсит как `{CMD}:{значение}` (цвет, число).
4. Вызывает `Flasher.stopGame(text, win)` — останавливает колесо на нужном числе.

```python
# Фильтр входящих транзакций на стороне клиента (упрощённо)
def should_process(event, my_address, server_addresses):
    tx   = event["transaction"]
    meta = event["meta"]
    return (
        event["type"] == "transaction"
        and tx.get("Destination") == my_address          # адресовано мне
        and tx.get("Account") not in server_addresses    # не от меня самого
        and meta.get("TransactionResult") == "tesSUCCESS"
        and "Memos" in tx
    )
```

> **Важно:** клиент фильтрует по признаку `Destination != SERVER_ADDRESS_ROULETTE`.  
> Поскольку три адреса — один, сервер должен отвечать транзакцией **с адреса сервера** на **адрес игрока**, чтобы клиент её обработал.

---

## 10. Таймаут ожидания ответа

Клиент показывает крутящееся колесо и ждёт ответа сервера. Пользователь может настроить таймаут в настройках приложения: **1 / 2 / 3 / 5 минут** (по умолчанию — 2 минуты).

При срабатывании таймаута:
- Клиент показывает **нейтральный экран** ("NO RESPONSE") — без Win/Lost оформления.
- WebSocket **продолжает слушать**. Если сервер ответит позже:
  - Если пользователь ещё на экране Flasher — ответ **игнорируется** (результат уже показан).
  - Если пользователь вернулся в MainActivity — открывается Win-экран или Lost-экран в штатном режиме.
- **Деньги в безопасности** — транзакция уже в блокчейне, проверить можно в истории.

> Бэкенду не нужно делать ничего особенного — таймаут полностью на стороне клиента.  
> Ответ сервера через любое время всё равно будет обработан корректно.

---

## 11. Сводная таблица всех команд

### Исходящие (клиент → сервер)

| Команда (MemoData) | Игра | Сумма | Адрес назначения |
|---------------------|------|-------|-----------------|
| `BET:RED:{ref}` | Угадай цвет | 0.1–100 XRP | `rGrEJZaBFYhPGuyM7NiJbJw2yXVB9vJHah` |
| `BET:BLK:{ref}` | Угадай цвет | 0.1–100 XRP | `rGrEJZaBFYhPGuyM7NiJbJw2yXVB9vJHah` |
| `BET:N:{1-36}:{ref}` | Угадай число | 0.1–100 XRP | `rfcMxSEz4JP8zj65LU5Nw9hKfpEaD6Ss9` |
| `BET:R:{ставки}:{ref}` | Рулетка | сумма ставок, макс 100 XRP | `rGrEJZaBFYhPGuyM7NiJbJw2yXVB9vJHah` |
| `REF` | Стать рефералом | **66 XRP** | `rGrEJZaBFYhPGuyM7NiJbJw2yXVB9vJHah` |
| `REF:REC` | Восстановить реферала | **13 XRP** | `rGrEJZaBFYhPGuyM7NiJbJw2yXVB9vJHah` |

### Входящие (сервер → клиент) — Угадай цвет и Угадай число

| Команда (MemoData) | Когда | Сумма транзакции |
|--------------------|-------|-----------------|
| `WIN:{число}` | Выигрыш | итоговая выплата в XRP |
| `LOSE:{число}` | Проигрыш | **12 дропов** (минимум) |
| `JKPT:{число}` | Джекпот-выигрыш | итоговая выплата |
| `LOTO:{число}` | Лото-выигрыш | итоговая выплата |

### Входящие (сервер → клиент) — Рулетка и реферальная программа

| Команда (MemoData) | Когда | Сумма транзакции |
|--------------------|-------|-----------------|
| `RLT:{число}:WIN` | Выигрыш рулетки | итоговая выплата в XRP |
| `RLT:{число}:LOSE` | Проигрыш рулетки | **12 дропов** (минимум) |
| `RLT:LOTTO:DAILY:{дата}` | Победитель ежедневной лотереи | джекпот |
| `RLT:LOTTO:SUPER:{год-месяц}` | Победитель ежемесячной лотереи | джекпот |
| `RLT:REF:CODE:{код}` | Реферальный код выдан / восстановлен | символическая |
| `RLT:REF:REWARD:{код}` | Вознаграждение рефереру | сумма вознаграждения |

---

## 12. Лимиты и правила

| Параметр | Значение |
|----------|---------|
| Мин. ставка (Цвет, Число, Рулетка) | 0.1 XRP |
| Макс. ставка (Цвет, Число) | 100 XRP |
| Макс. суммарная ставка (Рулетка) | 100 XRP |
| Точность суммы | 6 знаков после запятой (дропы) |
| Стоимость реферальной регистрации | 66 XRP |
| Стоимость восстановления | 13 XRP |
| Макс. количество рефералов | 666 666 |
| Проигрышная транзакция (обязательна) | ≥ 12 дропов |
| Рекомендуемое время ответа | 3–8 секунд |
| Таймаут ожидания (настройка клиента) | 1 / 2 / 3 / 5 минут |

---

## 13. Пример полного цикла — Угадай цвет

```
1. Игрок ставит 5 XRP на красный, реферал "XURA99":

   Destination: rGrEJZaBFYhPGuyM7NiJbJw2yXVB9vJHah
   Amount:      5 000 000 drops
   MemoData:    HEX("BET:RED:XURA99")

2. Сервер генерирует число рулетки: выпало 17 (красное)

3. Сервер отвечает:
   Destination: <адрес кошелька игрока>
   Amount:      10 000 000 drops  (5 × 2 = 10 XRP)
   MemoData:    HEX("WIN:17")

4. Клиент получает транзакцию через WebSocket,
   останавливает колесо на числе 17, показывает "Выигрыш".
```

---

## 14. Пример полного цикла — Рулетка

```
1. Игрок ставит: число 17 (1.5 XRP) + красные (2.0 XRP), итого 3.5 XRP:

   Destination: rGrEJZaBFYhPGuyM7NiJbJw2yXVB9vJHah
   Amount:      3 500 000 drops
   MemoData:    HEX("BET:R:n17@1.5,r@2.0:0")

2. Сервер крутит рулетку: выпало 17 (красное)

3. Выигрыш: n17 → 1.5 × 36 = 54 XRP; r → 2.0 × 2 = 4 XRP; итого 58 XRP

4. Сервер отвечает:
   Destination: <адрес игрока>
   Amount:      58 000 000 drops
   MemoData:    HEX("RLT:17:WIN")

5. Клиент: колесо останавливается на 17, показывает Win-экран с суммой.
```

---

## 15. Пример полного цикла — Реферальная программа

```
1. Игрок нажимает "Стать рефералом":

   Destination: rGrEJZaBFYhPGuyM7NiJbJw2yXVB9vJHah
   Amount:      66 000 000 drops
   MemoData:    HEX("REF")

2. Сервер регистрирует кошелёк, выдаёт код:

   Destination: <адрес игрока>
   Amount:      <символическая комиссия>
   MemoData:    HEX("RLT:REF:CODE:7483647")

3. Клиент сохраняет "7483647" как реферальный код.
   Другие игроки вписывают этот код при ставке.

4. Когда кто-то использует код 7483647, сервер присылает рефереру:

   Destination: <адрес владельца кода>
   Amount:      <сумма вознаграждения>
   MemoData:    HEX("RLT:REF:REWARD:7483647")

5. Клиент: если Flasher открыт — доставляет позже; иначе — сразу Win-экран.
```

```
1. Игрок нажимает "Восстановить реферала":

   Destination: rGrEJZaBFYhPGuyM7NiJbJw2yXVB9vJHah
   Amount:      13 000 000 drops
   MemoData:    HEX("REF:REC")

2. Сервер ищет код, ранее привязанный к адресу, и отвечает:

   MemoData:    HEX("RLT:REF:CODE:7483647")
```

---

## 16. Резерв активации кошелька XRPL

XRPL требует **10 XRP резерва** для активации каждого аккаунта.  
Клиент **всегда вычитает 10 XRP** из полного баланса при отображении доступной суммы.

```java
// PaymentAndSocketManagerXRPL.getBalance()
доступный_баланс = полный_баланс − 10.000000 XRP
```

Если `полный_баланс ≤ 10 XRP` — клиент показывает `0.000000 XRP` и блокирует ставки.

**Что это значит для бэкенда:**
- Получив ставку от игрока, не удивляться, что на его адресе числится на 10 XRP больше, чем ставка.
- Минимальное пополнение нового кошелька для начала игры: **> 10 XRP** (резерв) + сумма ставки + комиссия транзакции (~0.000012 XRP).

---

## 17. Обновление баланса по WebSocket

При каждой входящей транзакции клиент **параллельно** запускает два потока:
1. `NotifierRun` — разбирает мемо, показывает результат игры.
2. `UpdateBalanceRun` — обновляет отображаемый баланс.

`UpdateBalanceRun` читает итоговый баланс аккаунта **напрямую из события транзакции** (без отдельного запроса):

```json
event["meta"]["AffectedNodes"][1]["ModifiedNode"]["FinalFields"]["Balance"]
```

Значение в дропах, клиент переводит в XRP и вычитает резерв 10 XRP.

> **Важно для бэкенда:** структура `AffectedNodes` в XRPL не гарантирует порядок элементов.  
> Клиент обращается к индексу `[1]` — если сервер генерирует транзакции нестандартной структуры,  
> обновление баланса может упасть с `JSONException` (обработано через try/catch, не критично —  
> баланс обновится при следующем запросе).

---

## 18. Зарезервированные коды (не обрабатываются клиентом)

Следующие коды определены в приложении, но текущий парсер (`NotifierRun`) их **не обрабатывает** — они игнорируются:

| Код | Константа | Возможное назначение |
|-----|-----------|---------------------|
| `22` | `REFUND` | Возврат ставки (например, при ошибке сервера) |
| `99` | `DONATION` | Пожертвование / благодарность без игрового контекста |

Если сервер отправит мемо `REFUND:22` или `DONATION:99` — клиент получит транзакцию (баланс обновится), но никакого уведомления на экране **не появится**. Колесо продолжит крутиться до таймаута.

---

## 19. Критические детали парсинга

### 19.1 Разбивка мемо на стороне клиента

```java
// NotifierRun.java — упрощённо
String memoText = new String(hexToBytes(hexMemo), StandardCharsets.UTF_8).toUpperCase();

if (memoText.startsWith("RLT:")) {
    String[] parts = memoText.split(":");
    // parts[1] определяет подтип:
    //   "LOTTO" → лотерея (parts[2] = DAILY/SUPER, parts[3] = дата)
    //   "REF"   → реферал (parts[2] = CODE/REWARD, parts[3] = код)
    //   иначе   → рулетка (parts[1] = число, parts[2] = WIN/LOSE)
} else {
    // CMD:значение для остальных игр
    String[] parts = memoText.split(":", 2);
    String cmd          = parts[0];             // WIN, LOSE, JKPT, LOTO, REF
    String serverNumber = parts.length > 1 ? parts[1] : "0";
}
```

Примеры разбивки:

| Мемо | `parts` |
|------|---------|
| `RLT:17:WIN` | `["RLT", "17", "WIN"]` |
| `RLT:17:LOSE` | `["RLT", "17", "LOSE"]` |
| `RLT:LOTTO:DAILY:2026-06-30` | `["RLT", "LOTTO", "DAILY", "2026-06-30"]` |
| `RLT:LOTTO:SUPER:2026-06` | `["RLT", "LOTTO", "SUPER", "2026-06"]` |
| `RLT:REF:CODE:7483647` | `["RLT", "REF", "CODE", "7483647"]` |
| `RLT:REF:REWARD:7483647` | `["RLT", "REF", "REWARD", "7483647"]` |
| `WIN:17` | `["WIN", "17"]` |
| `LOSE:0` | `["LOSE", "0"]` |

> Дата `2026-06-30` содержит дефисы, а не двоеточия — `split(":")` возвращает её целиком в `parts[3]`.

### 19.2 Uppercase

```java
String memoText = new String(hexToBytes(hexMemo), StandardCharsets.UTF_8).toUpperCase();
```

Клиент **принудительно переводит мемо в uppercase** перед парсингом.  
Сервер может отправлять мемо в любом регистре — `rlt:17:win`, `RLT:17:WIN` — всё обработается одинаково.

### 19.3 Фильтр ответных транзакций

```java
// NotifierRun.run()
if (message.getJSONObject("transaction").has("Memos")
    && !message.getJSONObject("transaction").getString("Destination")
        .equals(NetworkConfig.SERVER_ROULETTE)) {
    responseToBet(message);
}
```

Клиент обрабатывает транзакцию если:
1. В ней есть `Memos` — это ответ от сервера.
2. `Destination` **не равен** `rGrEJZaBFYhPGuyM7NiJbJw2yXVB9vJHah` — чтобы не обработать собственную исходящую ставку повторно.

### 19.4 Защита от двойного вызова

Если `gameResultShown == true` — любой повторный вызов `stopGame()` игнорируется. Это защищает от гонки: таймаут сработал, а потом пришёл запоздалый ответ сервера (или наоборот).

---

## 20. Таймлайн и идемпотентность

### 20.1 Время ответа

Клиент показывает крутящееся колесо с настраиваемым таймаутом (1–5 минут).  
Рекомендуемое время ответа сервера: **3–8 секунд** (имитирует спин рулетки).

### 20.2 Дедупликация

Каждая транзакция на XRPL имеет уникальный `hash`. Бэкенд обязан:

```
1. Получить входящую транзакцию
2. Проверить hash в базе обработанных — если уже есть, игнорировать
3. Записать hash как "в обработке"
4. Рассчитать результат и отправить ответ
5. Записать hash как "обработано"
```

### 20.3 Подписка бэкенда на XRPL WebSocket

```json
{
  "command": "subscribe",
  "streams": ["transactions"],
  "accounts": [
    "rGrEJZaBFYhPGuyM7NiJbJw2yXVB9vJHah",
    "rfcMxSEz4JP8zj65LU5Nw9hKfpEaD6Ss9"
  ]
}
```

Фильтр входящих ставок:
```python
def is_valid_bet(event):
    tx   = event["transaction"]
    meta = event["meta"]
    return (
        event.get("type") == "transaction"
        and meta.get("TransactionResult") == "tesSUCCESS"
        and "Memos" in tx
        and tx.get("Destination") in SERVER_ADDRESSES
    )
```

---

## 21. Кодирование MemoData

```python
def encode_memo(text: str) -> str:
    return text.encode("utf-8").hex().upper()

def decode_memo(hex_str: str) -> str:
    return bytes.fromhex(hex_str).decode("utf-8").upper()

# Примеры
encode_memo("BET:RED:0")                    # → "4245543A5245443A30"
encode_memo("BET:N:17:XURA")               # → "4245543A4E3A31373A58555241"
encode_memo("BET:R:n5@1.5,r@2.0:0")       # → "..."
encode_memo("RLT:17:WIN")                   # → "524C543A31373A57494E"
encode_memo("RLT:17:LOSE")                  # → "524C543A31373A4C4F5345"
encode_memo("RLT:LOTTO:DAILY:2026-06-30")  # → "..."
encode_memo("RLT:LOTTO:SUPER:2026-06")     # → "..."
encode_memo("RLT:REF:CODE:7483647")        # → "..."
encode_memo("RLT:REF:REWARD:7483647")      # → "..."
encode_memo("REF")                          # → "524546"
encode_memo("REF:REC")                      # → "5245463A524543"
```

---

## 22. Полная схема протокола (одна страница)

```
╔══════════════════════════════════════════════════════════════════════╗
║                    XURA — ПРОТОКОЛ СТАВОК (XRPL)                   ║
╠══════════════════════════════════════════════════════════════════════╣
║  ИСХОДЯЩИЕ (клиент → сервер)                                        ║
║                                                                      ║
║  Угадай цвет:    BET:RED:{ref}  или  BET:BLK:{ref}                 ║
║  Угадай число:   BET:N:{1-36}:{ref}                                 ║
║  Рулетка:        BET:R:{код@сумма,...}:{ref}                        ║
║  Стать рефералом:  REF                (66 XRP)                      ║
║  Восстановить:     REF:REC            (13 XRP)                      ║
║                                                                      ║
╠══════════════════════════════════════════════════════════════════════╣
║  ВХОДЯЩИЕ — Угадай цвет / Угадай число (формат CMD:)                ║
║                                                                      ║
║  WIN:{число}     выигрыш — колесо на {число}                        ║
║  LOSE:{число}    проигрыш — колесо на {число}                       ║
║  JKPT:{число}    джекпот (= WIN)                                    ║
║  LOTO:{число}    лото-выигрыш (= WIN)                               ║
║                                                                      ║
╠══════════════════════════════════════════════════════════════════════╣
║  ВХОДЯЩИЕ — Рулетка и реферальная программа (формат RLT:)           ║
║                                                                      ║
║  RLT:{n}:WIN              выигрыш рулетки, колесо на {n}            ║
║  RLT:{n}:LOSE             проигрыш рулетки, колесо на {n}           ║
║  RLT:LOTTO:DAILY:{дата}   победитель ежедневной лотереи             ║
║  RLT:LOTTO:SUPER:{месяц}  победитель ежемесячной лотереи            ║
║  RLT:REF:CODE:{код}       реферальный код выдан / восстановлен      ║
║  RLT:REF:REWARD:{код}     вознаграждение рефереру                   ║
║                                                                      ║
╠══════════════════════════════════════════════════════════════════════╣
║  СУММЫ И ПРАВИЛА                                                     ║
║                                                                      ║
║  LOSE — отправить минимум 12 дропов (0.000012 XRP).                 ║
║  Колесо ждёт транзакцию в любом случае (таймаут клиента 1–5 мин).  ║
║  Регистр не важен — клиент приводит к UPPERCASE перед парсингом.    ║
║                                                                      ║
╠══════════════════════════════════════════════════════════════════════╣
║  БАЛАНС                                                              ║
║                                                                      ║
║  Клиент вычитает 10 XRP (резерв XRPL) из отображаемого баланса.    ║
║  Реальный баланс на ноде = показанный_баланс + 10 XRP.              ║
╚══════════════════════════════════════════════════════════════════════╝
```
