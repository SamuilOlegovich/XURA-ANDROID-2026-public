# Flutter vs React Native — для портирования XURA на iOS

> Контекст: XURA — Java/Android, MVVM, Dagger Hilt, xrpl4j, 31 экран, 79+ тестов.  
> Протокол задокументирован: [`BACKEND_PROTOCOL.md`](./BACKEND_PROTOCOL.md) v1.2  
> Цель: одной кодовой базой покрыть Android + iOS.

---

## Что изменилось с первичного анализа (2026-06-17)

| Изменение | Влияние на оценку |
|-----------|-----------------|
| `BACKEND_PROTOCOL.md` задокументирован (v1.2) | Снижает неопределённость XRPL-клиента в обоих фреймворках |
| Протокол рулетки: новые форматы `RLT:` (6 команд) | Чуть сложнее парсинг, но всё задокументировано |
| `RouletteWheelView` — кастомная Canvas-отрисовка | Критичный артефакт: требует `CustomPainter` (Flutter) или Skia/SVG (RN) |
| NumberPicker (барабан) удалён — только чипы/слайдер | Меньше нестандартного UI для переноса |
| Таймаут ожидания сервера (1–5 мин, настройки) | Стандартная логика `Timer`, нейтрально для обоих |
| Биометрия (PBKDF2 + Keystore) | Уже есть готовые пакеты в обоих фреймворках |

---

## 🐦 Flutter (Google, язык Dart)

**Как работает:** рисует весь UI через собственный движок (Skia/Impeller) —
не использует нативные компоненты iOS/Android, рисует каждый пиксель сам.

### Плюсы
- Один код → Android + iOS (+ Web, Desktop при желании)
- Архитектура MVVM переносится 1:1 (BLoC / Riverpod ≈ ViewModel + LiveData)
- UI выглядит **одинаково** на обеих платформах — что нарисовал, то и увидишь
- Dart — строго типизированный, синтаксис очень близок к Java (2–3 недели на освоение)
- Hot reload — правишь код, видишь результат мгновенно
- **`CustomPainter` + `AnimationController`** — прямой аналог `RouletteWheelView`.
  Canvas API один в один: `drawArc`, `drawLine`, `drawCircle`, матрицы трансформаций.
  Портирование сводится к переводу Java→Dart без концептуальных изменений.
- Хорошие пакеты для кошелька:
  - `flutter_secure_storage` — замена Android Keystore / iOS Keychain
  - `local_auth` — биометрия (Touch ID / Face ID)
  - `mobile_scanner` — QR-сканирование
  - `web_socket_channel` — WebSocket к XRPL
- Отличная производительность (нет JS-моста, нативная компиляция)

### Минусы
- Dart — не Java, придётся учить (хотя порог низкий)
- UI **не нативный** — кнопки и анимации "флаттеровские", не стандартные iOS/Android
- Бинарник тяжелее (~15–20 МБ overhead от Flutter engine)
- XRPL-библиотека для Dart менее зрелая, чем xrpl4j или xrpl.js — часть логики
  придётся писать вручную (JSON-RPC/WebSocket по [`BACKEND_PROTOCOL.md`](./BACKEND_PROTOCOL.md) несложный, но время)

### Оценка переноса XURA
| Блок | Время | Примечание |
|------|-------|-----------|
| XRPL-клиент + WebSocket + протокол `RLT:` | 3–5 недель | Протокол задокументирован — меньше угадывания |
| `RouletteWheelView` → `CustomPainter` | 1 неделя | Canvas API 1:1 |
| UI (31 экран → Flutter виджеты) | 5–7 недель | Барабан убран — меньше экзотики |
| Безопасность (Keystore → flutter_secure_storage) | 1 неделя | |
| **Итого** | **~2.5–3.5 месяца** | |

---

## ⚛️ React Native (Meta, язык TypeScript)

**Как работает:** компилирует JS-компоненты в **настоящие нативные** виджеты
iOS и Android. Кнопка на iOS — это реальная UIButton, не нарисованная.

### Плюсы
- **xrpl.js** — официальная библиотека от Ripple, самая зрелая и полная.
  Вся логика кошелька/транзакций уже написана и проверена сообществом.
  Задокументированный протокол (`RLT:`, `CMD:`) легко покрывается парсером в TS.
- UI выглядит **нативно** на каждой платформе — iOS как iOS, Android как Android
- TypeScript — строгая типизация, огромная экосистема npm
- Expo — надстройка над RN, убирает боль с Xcode/Gradle конфигурациями
- Богатая Web3/крипто экосистема: кошельки, подписи, шифрование — всё есть
- Пакеты для кошелька:
  - `react-native-keychain` — биометрия + безопасное хранилище
  - `vision-camera` — QR-сканирование
  - `@react-native-community/netinfo` — состояние сети
  - `react-native-quick-crypto` — нативная криптография (быстро)

### Минусы
- JavaScript/TypeScript — другая парадигма. Хуки (useState, useEffect) отличаются
  от LiveData. Нужно перестроить мышление.
- **`RouletteWheelView` — главная проблема.** Кастомная Canvas-анимация требует
  `react-native-skia` или `react-native-svg` + `react-native-reanimated`.
  Три разных библиотеки вместо одного `CustomPainter`; отладка сложнее.
- Производительность — JS-поток и нативный поток разделены. Сложные анимации
  и крипто-операции в JS могут лагать (решается нативными модулями)
- Отладка сложнее — два мира (JS + нативный), stacktrace иногда непонятные
- `node_modules` — исторически больное место, зависимости конфликтуют
- Новая архитектура (JSI + Fabric) лучше старого Bridge, но не все библиотеки
  успели обновиться

### Оценка переноса XURA
| Блок | Время | Примечание |
|------|-------|-----------|
| XRPL-клиент (xrpl.js уже готов!) | 1–2 недели | Парсинг `RLT:` / `CMD:` — несколько десятков строк TS |
| `RouletteWheelView` → Skia / SVG + Reanimated | 2–3 недели | Три библиотеки, нет прямого аналога |
| UI (31 экран → React компоненты) | 5–7 недель | Барабан убран — меньше экзотики |
| Безопасность (react-native-keychain) | 1 неделя | |
| **Итого** | **~2.5–3.5 месяца** | |

---

## ⚖️ Прямое сравнение для XURA

| Критерий | Flutter | React Native |
|---|---|---|
| XRPL-библиотека | Слабее, частично самописная | **xrpl.js — официальная, зрелая** |
| Протокол задокументирован | Нейтрально | Нейтрально |
| Язык | Dart (близкий к Java) | TypeScript (другая парадигма) |
| UI нативность | Свой движок, одинаков везде | Настоящие нативные компоненты |
| Производительность | **Отличная** (нет JS bridge) | Хорошая (с JSI), хуже для крипто |
| `RouletteWheelView` | **`CustomPainter` — прямой аналог** | Skia/SVG + Reanimated — сложнее |
| Анимации колеса | **Отличные, 1–2 нед.** | Хорошие, 2–3 нед. |
| Экосистема крипто/Web3 | Слабее | **Намного богаче** |
| Порог входа с Java | Низкий (Dart похож) | Средний (JS — другой мир) |
| Итоговое время | ~2.5–3.5 месяца | ~2.5–3.5 месяца |

> **Оценки сравнялись** — документация протокола снизила неопределённость XRPL-стороны,
> а RouletteWheelView добавила сложности Flutter стороне RN.

---

## 🎯 Вывод

**React Native** — если хочешь быстрее и надёжнее в части XRPL.
xrpl.js написана Ripple, хорошо документирована, все операции с кошельком
уже там. Парсинг протокола `RLT:`/`CMD:` — пара десятков строк TypeScript.

**Flutter** — если важнее контроль над UI и анимации.
`RouletteWheelView` переносится 1:1 через `CustomPainter` за неделю.
В React Native то же самое потребует координации трёх библиотек (Skia, SVG, Reanimated).

### Личная рекомендация

**React Native + Expo + TypeScript + xrpl.js** — xrpl.js всё ещё экономит
недели работы на самом рискованном куске, а `RouletteWheelView` на react-native-skia
решаемо, просто дольше.

Если планируется Web-версия (DApp) — переход от xrpl.js к браузеру почти бесшовный.  
Если планируется **Desktop** (Windows/macOS) — Flutter выигрывает вчистую.

---

## 📋 Чеклист переноса (общий для обоих)

- [ ] WebSocket-подписка на `accounts` → парсинг `RLT:` и `CMD:` форматов
- [ ] Отправка Payment-транзакций с MemoData (HEX-кодирование)
- [ ] PBKDF2 + биометрия + безопасное хранилище seed-фразы
- [ ] `RouletteWheelView` — кастомная Canvas-анимация с `stopAtNumber(n)`
- [ ] Таймаут ожидания сервера (настройка 1/2/3/5 мин) — нейтральный экран при срабатывании
- [ ] 31 экран: игры (цвет, число, рулетка), настройки, история, реферальная программа
- [ ] История транзакций — иконки по префиксу мемо (`RLT:` → рулетка, `BET:RED` → цвет, …)
- [ ] Локализация: 10 языков (en, ru, fr, es, de, pt, zh, ar, bn, hi)

---

*Первичный анализ: 2026-06-17 · Обновлено: 2026-06-30*
